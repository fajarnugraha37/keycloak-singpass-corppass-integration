<!DOCTYPE html>
<html lang="{{ coalesce lang 'en' }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    {{> head}}
</head>
<body
    {{#if cssBody}}
        class="{{ cssBody }}" 
    {{/if}}
    {{#if cssId}}
        id="{{ cssId }}" 
    {{/if}}
    >
    {{> body }}
</body>
</html>